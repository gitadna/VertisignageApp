<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug only: cleartext HTTP allowed for LAN dev. Release uses main manifest rules. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
