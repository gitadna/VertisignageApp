<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Kiosk deployments can run over LAN HTTP; allow cleartext to avoid media fetch blocks. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
